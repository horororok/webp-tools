# Node
node_modules/
*.log
npm-debug.log*
.pnpm-debug.log*
.pnpm-store/

# tsup / typescript 산출물 (소스에서 재생성)
dist/
*.tsbuildinfo

# 에디터/OS
.DS_Store
.idea/
.vscode/
*.swp

# 환경
.env
.env.local

# 빌드 부산물(컨테이너 외부로 새어 나오면 안 되는 임시물)
build/.cache/

# wasm/ 와 licenses/ 는 의도적으로 커밋함(벤더링).
# 따라서 ignore 하지 않는다.

# 문서
docs/*