packages:
  - "packages/*"
  - "examples/*"

# pnpm 10+에서 install-time 빌드 스크립트는 명시적 허용 필요.
# esbuild는 tsdown -> unrun -> bundle-require 체인이 TS/JS config 로드용으로
# 의존하는 네이티브 바이너리(postinstall로 받음).
allowBuilds:
  esbuild: true
